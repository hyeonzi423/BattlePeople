const NotifyCode = new Map<number, string>([
	[-1, "Notify"], // 서버에서 주지 않지만 메뉴를 위해 할당한 값
	[0, "Invite"],
	[1, "Live"],
	[2, "Live"],
	[3, "Live"],
	[4, "Live"],
	// [4, "Punishment"],
]);

export default NotifyCode;
